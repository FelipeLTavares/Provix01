import React from "react";

interface MapsProps {
  coordinates: [number, number];
  mapId: string;
}

const Maps: React.FC<MapsProps> = ({ coordinates, mapId }) => {
  return (
    <></>
  );
};

export default Maps;
